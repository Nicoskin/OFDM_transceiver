#include <complex>
#include <vector>

int nearest_power_of_two(int n);
std::vector<std::complex<double>> fft(const std::vector<std::complex<double>> &num);
std::vector<std::complex<double>> ifft(const std::vector<std::complex<double>> &num);
std::vector<std::complex<double>> fftshift(const std::vector<std::complex<double>> &num);
